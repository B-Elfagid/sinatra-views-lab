<!DOCTYPE html>
 <html>
   <head>
     <title>current date and time</title>
   </head>
  <body>
    <% today = DateTime.now.strftime("The date is %A, %B %d, %Y") %>
    <p><%= today %></p>
  </body>

</html>